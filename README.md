# ChatGPT-Explain-Linux

To run:

1. Set openai apikey variable: `export OPENAI_API_KEY=<YOUR_KEY_HERE>`
2. Give bash script sudo: `chmod +x makecommand.sh`
3. Run bash script to make `chatgpt` a command `./makecommand.sh`